package com.example.demo.Service;

import com.example.demo.DTO.BoardDto;
import java.util.List;
import java.util.Map;

public interface BoardService {
	public List<BoardDto> getBoardList();
	public String Boardinsert(Map<String, String> params);
	public BoardDto BoardGet(Map<String,String> params);
	public String BoardDelete(Map<String,String> params);
	public String BoardUpdate(Map<String,String> params);
}

//Service인터페이스
//추상 클래스 : 추상클래스란, 하나 이상의 추상 메소드를 포함한 클래스를 추상 클래스(abstract class)
//하나 이상의 추상 메서드만 포함하면 되며 생성자, 일반 메서드도 포함 가능
//부모 클래스에서 정의한 동작을 자식 클래스에서 구현하고 그것을 자식이 구현하고 확장(js class extend?)
//인터페이스와 달리 다른 기능도 확장하고 추가할 수 있음.

//인터페이스 : 어떠한설계도 / 설계도의 규칙에 맞추어 개발 하며 그규칙에 맞춰 개발할 시 내부구현을 알 필요
//가 없이 인터페이스 함수가 잘 동작할 것이라고 호출

